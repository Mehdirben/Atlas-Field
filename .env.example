# 🔐 Required for Core Setup (Mandatory)
# NEXTAUTH_URL should match your local development URL (usually http://localhost:3000)
NEXTAUTH_URL=http://localhost:3000
# Generate a random string: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# 🗺️ External Services (Required for full demo experience)
# Get a key at maptiler.com for the health maps
NEXT_PUBLIC_MAPTILER_KEY=your_maptiler_key_here
# Get a key at aistudio.google.com for the Site Assistant
GEMINI_API_KEY=your_gemini_api_key_here

# 📡 Satellite Data (Optional in Mock Mode)
# Only needed if you want to test adding new real sites
SENTINEL_HUB_CLIENT_ID=your_id_here
SENTINEL_HUB_CLIENT_SECRET=your_secret_here

# 🖥️ Backend API (Optional in Mock Mode)
# Only needed if USE_MOCK is set to FALSE in the code
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
INTERNAL_API_URL=http://backend:8000/api/v1
